{
  "name": "schei",
  "private": true,
  "description": "Schei — local-first envelope budgeting: YNAB-style engine, AI assistant, reports, all on your machine.",
  "scripts": {
    "setup": "node scripts/start-all.mjs --setup-only",
    "start": "node scripts/start-all.mjs",
    "icon": "node scripts/desktop-icon.mjs",
    "test:backend": "npm test --prefix backend",
    "test:frontend": "npm test --prefix frontend"
  },
  "engines": {
    "node": ">=20"
  }
}
